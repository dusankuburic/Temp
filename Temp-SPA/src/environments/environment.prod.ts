export const environment = {
  production: true,
  // For Docker local deployment, using localhost:5001
  // TODO: Update this to your actual production domain when deploying to production
  apiUrl: 'https://localhost:5001/api/'
};
